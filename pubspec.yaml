name: ag
description: "A new Flutter package project."
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
    
  # dio_cache_interceptor: ^3.5.1

  dio: ^5.8.0+1
  get_it: ^8.0.3
  connectivity_plus: ^6.1.3

  logs:
    git:
      url: https://github.com/quochoang228/logs.git
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
